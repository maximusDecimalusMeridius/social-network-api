// bring in mongoose dependency
const mongoose = require("mongoose");

// define mongoose schema
const userSchema = new mongoose.Schema({
    
});

// declare the model object
const User = ("User", xSchema);

module.exports = User;
